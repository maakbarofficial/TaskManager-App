{
  "name": "19--pern-todo-app",
  "version": "1.0.0",
  "scripts": {
    "build": "if-env TYPE=server && (cd backend && npm install) || (cd frontend && npm install)",
    "server": "node ./backend/server.js",
    "client": "cd frontend && npm run dev"
  },
  "dependencies": {
    "if-env": "^1.0.4"
  }
}